#locals {  
  # Common tags
  #common_tags = merge(var.tags, {
  #  Environment = var.environment
  #  Project     = var.project_name
  #  ManagedBy   = "Terraform"
  #  CreatedAt   = timestamp()
  #})
#}
